{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the bar window",
  "windows": ["bar"],
  "permissions": [
    "core:default",
    "opener:default",
    "core:window:default"
  ]
}
